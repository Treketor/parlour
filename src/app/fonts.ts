import { Schibsted_Grotesk } from "next/font/google";

// Variable font, so no weights are listed.
export const schibstedGrotesk = Schibsted_Grotesk({
  subsets: ["latin"],
  variable: "--font-schibsted",
  display: "swap",
});
